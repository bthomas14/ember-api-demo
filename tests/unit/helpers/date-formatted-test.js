import { dateFormatted } from '../../../helpers/date-formatted';
import { module, test } from 'qunit';

module('Unit | Helper | date formatted');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = dateFormatted(42);
  assert.ok(result);
});
